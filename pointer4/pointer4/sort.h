#ifndef __SORT_H__
#define __SORT_H__

#include <stdio.h>

void SwapCallByReference(int* a, int* b);
void sort(int* ptr);
void out(int* ptr);
void change(int array[], int* ptr);

#endif // !__SORT_H__
